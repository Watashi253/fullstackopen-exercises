{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Anandita",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "shx rm -rf dist && cd ../../part2/learning && npm run build && shx cp -r dist ../../part3/learning/dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"ui build\" && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
