{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Anandita",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "powershell Remove-Item -Recurse -Force dist && cd ../../part2/learning && npm run build && @powershell Copy-Item dist -Recurse ../../part3/learning/dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"ui build\" && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.7.2"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
